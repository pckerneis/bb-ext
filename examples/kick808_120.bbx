({
  double tb    = step_t(t, 120, 1, 0);
  double amp   = env_exp(0.20, tb);
  double prog  = 1.0 - exp(-50.0 * tb);
  double freq  = lerp(150.0, 50.0, prog);
  double body  = sine(t, freq) * amp;
  double click = 0.2 * env_exp(0.01, tb);
  body + click;
})